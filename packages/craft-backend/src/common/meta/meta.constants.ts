import type { MetaProtocol } from '@/common/meta/meta.types'

/**
 * Reflect metadata keys
 */
export const META_RES_OPTIONS_KEY = 'meta:res:options'
export const SKIP_META_RES_KEY = 'meta:res:skip'

/**
 * 默认 meta 字段值
 */
export const DEFAULT_PROTOCOL: MetaProtocol = 'default'
export const DEFAULT_SUCCESS_CODE = 0
export const DEFAULT_SUCCESS_MESSAGE = '请求成功'
