# 开发环境配置

# 后端 API 基础地址（与 craft-backend 的 PREFIX + VERSION 对齐）
VITE_API_HOST=http://localhost:3001/api

# 前端版本号（用于请求头标识，可选）
VITE_APP_VERSION=1.0.0

# CSRF 相关：后端目前未集成 CSRF 中间件，先留空
# 后端集成 CSRF 后填上对应的 cookie / header 名即可启用 axios 的 XSRF 行为
VITE_CSRF_COOKIE_NAME=
VITE_CSRF_HEADER_NAME=
